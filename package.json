{
  "name": "flux-grabber",
  "version": "1.0.0",
  "description": "Industrial video extraction tool.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Build skip'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "youtube-dl-exec": "^3.1.5"
  }
}
